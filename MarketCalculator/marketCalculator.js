// TODO : a function that returns calculations
// ? params : itemName, item price/kg, item quantity

const priceCalculator = (itemName, unitPrice, quantity) => {
  console.log(itemName, unitPrice, quantity);
};

priceCalculator("Bread", 20, 8);
